import 'package:flutter/material.dart';

class NewScreenWidget extends StatelessWidget {
  const NewScreenWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
