import 'package:flutter/material.dart';

class PopularScreenWidget extends StatelessWidget {
  const PopularScreenWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
